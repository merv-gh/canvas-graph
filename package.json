{
  "name": "ecs-canvas-graph",
  "version": "2.0.2",
  "description": "Keyboard-first, event-driven graph editor for writing and navigating structured ideas.",
  "license": "Apache-2.0",
  "private": true,
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": "^20.19.0 || ^22.12.0 || >=24.0.0"
  },
  "scripts": {
    "dx": "node dx/cli/dx.mjs",
    "dev": "node dx/cli/dev.mjs",
    "dev:frontend": "vite frontend --host 0.0.0.0 --port 5174 --strictPort",
    "build": "vite build frontend --base ./",
    "preview:build": "vite preview frontend",
    "requirements:graph": "node tools/requirements-graph.mjs",
    "build:lib": "vite build --config vite.lib.config.ts",
    "release:check": "npm run typecheck:release && npm run build && npm run test:dx && npm run test:performance && npm run test:release",
    "test": "npm run test:commands:coverage && npm run test:browser",
    "test:release": "npm run test:commands:coverage:full && npm run test:browser:full",
    "test:browser": "playwright test --config playwright.fast.config.cjs",
    "test:browser:full": "playwright test",
    "test:commands": "vitest run --config vitest.fast.config.ts",
    "test:commands:full": "vitest run",
    "test:commands:coverage": "COVERAGE=1 vitest run --config vitest.fast.config.ts --coverage",
    "test:commands:coverage:full": "COVERAGE=1 vitest run --coverage",
    "test:dx": "node dx/selftest.mjs",
    "test:performance": "PERF_ENFORCE=1 vitest run tests/commands/performance.test.ts tests/commands/probes/scale-10k.test.ts tests/commands/probes/telemetry-overhead.test.ts",
    "bench": "vitest run --config vitest.bench.config.ts",
    "bench:burn": "BURN=1 vitest run --config vitest.bench.config.ts",
    "perf:browser": "PERF_BROWSER=1 playwright test tests/perf-real.spec.ts --project=chromium --reporter=line",
    "perf:browser:report": "PERF_BROWSER=1 PERF_ENFORCE=0 playwright test tests/perf-real.spec.ts --project=chromium --reporter=line",
    "perf:input": "PERF_INPUT=1 playwright test tests/perf-input-delay.spec.ts --project=chromium --reporter=line",
    "typecheck": "tsgo --noEmit",
    "typecheck:release": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@typescript/native-preview": "7.0.0-dev.20260707.2",
    "@vitest/coverage-v8": "^4.1.8",
    "@webgpu/types": "^0.1.71",
    "jsdom": "^27.0.1",
    "pngjs": "^7.0.0",
    "typescript": "^5.8.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
