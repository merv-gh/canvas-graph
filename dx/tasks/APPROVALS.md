# Approvals

Task ids listed here are approved for `node walker/apply.mjs --task <id> --apply-for-real`
to apply the model's fix to the **real** repo (after the full quality gate passes:
suite + typecheck + 80% coverage in a fresh workspace).

This is the human gate: you watch the fix via `walker/preview.mjs`, then add its id
below to bless it. Lines starting with `#` are ignored.

## approved

# (add one task id per line under here, e.g.)
# choose-invert-shortcut
